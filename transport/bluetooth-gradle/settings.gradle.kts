//rootProject.name = "bluetooth-gradle"
rootProject.name = "com.example.bluetooth_gradle"

pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

include(":plugin")